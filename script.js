const yearEl = document.getElementById('year'); if (yearEl){ yearEl.textContent = new Date().getFullYear(); }
const cart = []; const cartUI = document.getElementById('cart-ui'); function renderCart(){ if (!cartUI) return; cartUI.innerHTML = ''; if (cart.length===0){ cartUI.innerHTML = '<div class="micro">Cart is empty.</div>'; return;} cart.forEach(i=>{const d=document.createElement('div'); d.className='cart-item'; d.innerHTML = `<span class="badge">${i.flavour}</span> × ${i.qty} — <strong>$${(11.99*i.qty).toFixed(2)}</strong>`; cartUI.appendChild(d);}); }
const add = document.getElementById('add'); if (add){ add.addEventListener('click',()=>{ const flavour = document.getElementById('flavour').value; const qty = parseInt(document.getElementById('qty').value||'1',10); cart.push({flavour,qty}); renderCart(); }); } renderCart();